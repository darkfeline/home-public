#!/bin/sh
# Send notifications to remote host
#
# Needs this in ssh_config:
#
#     RemoteForward /tmp/ssh_%l_dbus.sock /run/user/%i/bus
set -eu
host=$1
shift 1
DBUS_SESSION_BUS_ADDRESS="unix:path=/tmp/ssh_${host}_dbus.sock" notify-send "$@"
