#!/bin/bash
# EDITOR dispatch script
set -u

[[ -n ${INSIDE_EMACS:-} ]] && exec emacsclient "$@"

inpath() {
    command -v "$1" >/dev/null 2>&1
}

inpath ed && exec ed -p 'ed> ' "$@"

printf '%s: No editor available\n' "$0" >&2
exit 1
